import { ControlTypeCheckerPipe } from './control-type-checker.pipe';

describe('ControlTypeCheckerPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlTypeCheckerPipe();
    expect(pipe).toBeTruthy();
  });
});
